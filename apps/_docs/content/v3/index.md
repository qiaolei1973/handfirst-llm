# v3: 画曲线 — 进入非线性世界

> TODO: 内容待补充

## 上半场：当直线不够用时

1. 真实数据不总是直线 — `y = sin(x) + noise`
2. 多项式回归 — 猜"几次方"的痛苦
3. 线性堆线性还是线性 — 一行代数证明
4. ReLU: 折一下，打破线性
5. 多个 ReLU = 多条折线 = 逼近任意曲线
6. Layer = Linear + ReLU — 封装为基本单元

## 下半场：看看它的实际效果

7. 模型结构: 隐藏层 (N 个 ReLU) → 输出层
8. 损失函数 & 优化器 — 沿用 MSE + SGD
9. 反向传播（黑盒）— 名字和直觉，数学留给 v4
10. Dashboard 导览

---

> 运行: `pnpm dev:v3` → `http://localhost:3003`
