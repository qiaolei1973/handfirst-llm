{
  "name": "handfirst-llm",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter docs dev",
    "dev:v1": "pnpm exec tsx apps/v1/server.ts & pnpm --filter v1-app dev",
    "dev:v2": "pnpm exec tsx apps/v2/server.ts & pnpm --filter v2-app dev",
    "build": "pnpm --filter v1-app build && pnpm --filter v2-app build && pnpm --filter docs build"
  },
  "devDependencies": {
    "tsx": "^4.23.1"
  }
}
